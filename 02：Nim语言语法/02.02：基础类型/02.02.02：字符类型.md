在Nim语言中字符类型使用关键字char定义，字符类型和布尔类型一样也只占据一个字节，也因此它不能表示一个UTF-8的字符，但是它可以用于表示一个UTF-8字符的一部分。可以用单引号包住一个字符来为字符类型的变量设置值。诸如==，<，>，<=，>=这些操作符都可以用来操字符类型。$操作符可以把一个字符类型格式化成字符串类型。字符类型不能和数字类型混淆，要想得到一个字符类型的“序数值”，请使用ord方法。把一个数字类型的值格式化成字符类型，请使用chr方法。