在Nim语言中布尔类型使用bool关键字定义。布尔类型的变量占据一个字节的空间。布尔类型有且只两个值：true和false。布尔类型的变量用在while，if，elif，when这些流程控制语句中，用于判断是否满足条件。诸如：not，and，or，xor，<，<=，>，>=，!=，==这类操作符，计算的结果就是布尔类型的值。来看下面的例子：
while p != nil and p.name != "xyz":
  # p不为空并且p.name不等于xyz
  p = p.next
您如果对while语句和缩进的写法不是很明白，那么请不要着急，在流程控制章节，将会详细描述这部分内容。